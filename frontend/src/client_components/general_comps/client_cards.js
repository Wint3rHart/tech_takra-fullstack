
import useData from './useData';


const Packages = ({promise}) => {
let get=use

    return (
        <div className='flex h-[100vh] items-center m-6 ' ref={ref}>
            
            {data?.map((x,i)=>{return <Package_cards key={x.id} x={x} scroll={scrollYProgress} />})}
            
        </div>
    );
}